name: Build and Release

on:
  push:
    branches:
      - OTA_update
      - Release-branch
      - main
      - overhaul
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-war:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Update version in application.properties
        working-directory: ./billsoft
        run: |
          sed -i "s/app.version=.*/app.version=v1.0.${{ github.run_number }}/" src/main/resources/application.properties

      - name: Build with Maven and Run Regression Tests
        run: mvn clean package

      - name: Rename WAR file
        working-directory: ./billsoft
        run: cp target/billsoft-0.0.1-SNAPSHOT.war target/billsoft.war

      - name: Generate Checksum
        run: |
          sha256sum billsoft/target/billsoft.war | awk '{print $1}' > billsoft/target/billsoft.war.sha256

      - name: Package Regression Test Reports
        run: |
          mkdir -p reports-dist
          if [ -d "billsoft/target/site/jacoco" ]; then
            (cd billsoft/target/site && zip -r ../../../reports-dist/Regression-Test-Coverage-Report.zip jacoco)
          fi
          if [ -d "billsoft/target/surefire-reports" ]; then
            (cd billsoft/target && zip -r ../../reports-dist/Regression-Test-Results.zip surefire-reports)
          fi

      - name: Upload WAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: war-payload
          path: |
            billsoft/target/billsoft.war
            billsoft/target/billsoft.war.sha256
          if-no-files-found: error

      - name: Upload Regression Reports artifact
        uses: actions/upload-artifact@v4
        with:
          name: regression-reports
          path: reports-dist/*
          if-no-files-found: warn

  build-windows:
    runs-on: windows-latest
    needs: [build-war]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Download WAR payload
        uses: actions/download-artifact@v4
        with:
          name: war-payload
          path: downloaded-war

      - name: Build Launcher with Maven
        run: mvn clean package -pl launcher -DskipTests

      - name: Prepare jpackage input
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package-input
          New-Item -ItemType Directory -Force -Path package-input/runtime
          Copy-Item launcher/target/launcher-0.0.1-SNAPSHOT.jar package-input/launcher.jar
          Copy-Item downloaded-war/billsoft.war package-input/runtime/billsoft.war

      - name: Package Standalone Portable Zip
        shell: pwsh
        run: |
          jpackage --name RupeeCRM `
                   --input package-input `
                   --main-jar launcher.jar `
                   --main-class com.billing.simple.launcher.LauncherMain `
                   --type app-image `
                   --dest dist-pkg `
                   --app-version 1.0.${{ github.run_number }} `
                   --vendor "RupeeCRM" `
                   --java-options "-Djava.awt.headless=false"

          "@echo off`r`nstart `"`" `"%~dp0RupeeCRM.exe`"" | Out-File -Encoding ASCII -FilePath dist-pkg/RupeeCRM/Start-RupeeCRM.bat
          Copy-Item deleteall.bat dist-pkg/RupeeCRM/deleteall.bat

          Compress-Archive -Path dist-pkg/RupeeCRM/* -DestinationPath RupeeCRM-Windows-x64.zip

      - name: Package Windows Installer (MSI)
        shell: pwsh
        run: |
          # 1. Run jpackage with --temp to extract default WiX resources
          jpackage --name RupeeCRM `
                   --input package-input `
                   --main-jar launcher.jar `
                   --main-class com.billing.simple.launcher.LauncherMain `
                   --type msi `
                   --app-version 1.0.${{ github.run_number }} `
                   --vendor "RupeeCRM" `
                   --win-dir-chooser `
                   --win-menu `
                   --win-shortcut `
                   --win-per-user-install `
                   --java-options "-Djava.awt.headless=false" `
                   --temp wix-temp

          # 2. Inject LaunchApplication CustomAction into main.wxs so MSI starts service on install
          New-Item -ItemType Directory -Force -Path wix-resources
          if (Test-Path "wix-temp/main.wxs") {
              $actionXml = '<CustomAction Id="LaunchAppOnExit" Directory="INSTALLDIR" ExeCommand="&quot;[INSTALLDIR]RupeeCRM.exe&quot;" Execute="immediate" Impersonate="yes" Return="asyncNoWait" /><InstallExecuteSequence><Custom Action="LaunchAppOnExit" After="InstallFinalize">NOT Installed</Custom></InstallExecuteSequence></Product>'
              $wxs = Get-Content "wix-temp/main.wxs" -Raw
              $wxs = $wxs.Replace('</Product>', $actionXml)
              Set-Content -Path "wix-resources/main.wxs" -Value $wxs -Encoding UTF8

              # Remove initial MSI before rebuilding with custom action
              Remove-Item *.msi -Force -ErrorAction SilentlyContinue

              # 3. Build final MSI with auto-launch capability
              jpackage --name RupeeCRM `
                       --input package-input `
                       --main-jar launcher.jar `
                       --main-class com.billing.simple.launcher.LauncherMain `
                       --type msi `
                       --app-version 1.0.${{ github.run_number }} `
                       --vendor "RupeeCRM" `
                       --win-dir-chooser `
                       --win-menu `
                       --win-shortcut `
                       --win-per-user-install `
                       --java-options "-Djava.awt.headless=false" `
                       --resource-dir wix-resources
          }

          Get-ChildItem -Filter *.msi | Rename-Item -NewName RupeeCRM-Setup.msi

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-distribution
          path: |
            RupeeCRM-Windows-x64.zip
            RupeeCRM-Setup.msi
          if-no-files-found: error

  build-macos-arm64:
    runs-on: macos-14
    needs: [build-war]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Download WAR payload
        uses: actions/download-artifact@v4
        with:
          name: war-payload
          path: downloaded-war

      - name: Build Launcher with Maven
        run: mvn clean package -pl launcher -DskipTests

      - name: Prepare jpackage input
        run: |
          mkdir -p package-input/runtime
          cp launcher/target/launcher-0.0.1-SNAPSHOT.jar package-input/launcher.jar
          cp downloaded-war/billsoft.war package-input/runtime/billsoft.war
          xattr -cr package-input

      - name: Package macOS Apple Silicon (ARM64) DMG
        run: |
          jpackage --name "RupeeCRM" \
                   --input package-input \
                   --main-jar launcher.jar \
                   --main-class com.billing.simple.launcher.LauncherMain \
                   --type dmg \
                   --dest dist-pkg \
                   --app-version 1.0.${{ github.run_number }} \
                   --vendor "RupeeCRM" \
                   --mac-package-name "RupeeCRM" \
                   --mac-package-identifier "com.rupeecrm.billing" \
                   --java-options "-Djava.awt.headless=false"

          mv dist-pkg/*.dmg RupeeCRM-macOS-arm64.dmg

      - name: Sign and Notarize macOS ARM64 DMG
        if: env.APPLE_CERTIFICATE_P12_BASE64 != ''
        env:
          APPLE_CERTIFICATE_P12_BASE64: ${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/build.keychain
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          CERT_P12=$RUNNER_TEMP/certificate.p12
          echo "$APPLE_CERTIFICATE_P12_BASE64" | base64 --decode > "$CERT_P12"
          security import "$CERT_P12" -k "$KEYCHAIN_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          codesign --force --sign "Developer ID Application" --timestamp RupeeCRM-macOS-arm64.dmg

          xcrun notarytool submit RupeeCRM-macOS-arm64.dmg \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait

          xcrun stapler staple RupeeCRM-macOS-arm64.dmg

      - name: Upload macOS ARM64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-distribution
          path: RupeeCRM-macOS-arm64.dmg
          if-no-files-found: error

  build-macos-x64:
    runs-on: macos-13
    needs: [build-war]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Download WAR payload
        uses: actions/download-artifact@v4
        with:
          name: war-payload
          path: downloaded-war

      - name: Build Launcher with Maven
        run: mvn clean package -pl launcher -DskipTests

      - name: Prepare jpackage input
        run: |
          mkdir -p package-input/runtime
          cp launcher/target/launcher-0.0.1-SNAPSHOT.jar package-input/launcher.jar
          cp downloaded-war/billsoft.war package-input/runtime/billsoft.war
          xattr -cr package-input

      - name: Package macOS Intel (x86_64) DMG
        run: |
          jpackage --name "RupeeCRM" \
                   --input package-input \
                   --main-jar launcher.jar \
                   --main-class com.billing.simple.launcher.LauncherMain \
                   --type dmg \
                   --dest dist-pkg \
                   --app-version 1.0.${{ github.run_number }} \
                   --vendor "RupeeCRM" \
                   --mac-package-name "RupeeCRM" \
                   --mac-package-identifier "com.rupeecrm.billing" \
                   --java-options "-Djava.awt.headless=false"

          mv dist-pkg/*.dmg RupeeCRM-macOS-x64.dmg

      - name: Sign and Notarize macOS x64 DMG
        if: env.APPLE_CERTIFICATE_P12_BASE64 != ''
        env:
          APPLE_CERTIFICATE_P12_BASE64: ${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/build.keychain
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          CERT_P12=$RUNNER_TEMP/certificate.p12
          echo "$APPLE_CERTIFICATE_P12_BASE64" | base64 --decode > "$CERT_P12"
          security import "$CERT_P12" -k "$KEYCHAIN_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          codesign --force --sign "Developer ID Application" --timestamp RupeeCRM-macOS-x64.dmg

          xcrun notarytool submit RupeeCRM-macOS-x64.dmg \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait

          xcrun stapler staple RupeeCRM-macOS-x64.dmg

      - name: Upload macOS x64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-x64-distribution
          path: RupeeCRM-macOS-x64.dmg
          if-no-files-found: error

  release:
    runs-on: ubuntu-latest
    needs: [build-war, build-windows, build-macos-arm64, build-macos-x64]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download WAR artifact
        uses: actions/download-artifact@v4
        with:
          name: war-payload
          path: ./dist

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-distribution
          path: ./dist

      - name: Download macOS ARM64 artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-arm64-distribution
          path: ./dist

      - name: Download macOS x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-x64-distribution
          path: ./dist

      - name: Download Regression Reports
        uses: actions/download-artifact@v4
        with:
          name: regression-reports
          path: ./dist

      - name: Generate Release Notes
        run: |
          if [ -f billsoft/release-notes.txt ]; then
            cat billsoft/release-notes.txt > release-notes.txt
          else
            echo "## RupeeCRM Release v1.0.${{ github.run_number }}" > release-notes.txt
            echo "Professional CRM and billing background service with custom offline domain and bundled JRE." >> release-notes.txt
          fi
          echo "" >> release-notes.txt
          echo "### 📦 Distribution Artifacts" >> release-notes.txt
          echo "- **Windows Installer**: \`RupeeCRM-Setup.msi\`" >> release-notes.txt
          echo "- **Windows Portable**: \`RupeeCRM-Windows-x64.zip\`" >> release-notes.txt
          echo "- **macOS Apple Silicon (M1/M2/M3/M4)**: \`RupeeCRM-macOS-arm64.dmg\`" >> release-notes.txt
          echo "- **macOS Intel (x86_64)**: \`RupeeCRM-macOS-x64.dmg\`" >> release-notes.txt
          echo "- **WAR Payload**: \`billsoft.war\`" >> release-notes.txt
          echo "" >> release-notes.txt
          echo "> **Note on macOS Releases**: Production builds signed and notarized with Apple Developer ID provide zero-friction Drag-to-Applications installation. Builds without Apple signing secrets are provided as developer/preview artifacts." >> release-notes.txt
          echo "" >> release-notes.txt
          echo "### 🧪 Automated Quality & Regression Testing" >> release-notes.txt
          echo "- **Regression Test Pack**: 160 Tests (Billing, Accounting, Security, HR, Inventory, PO, PDFs, E2E)" >> release-notes.txt
          echo "- **Test Execution Results**: Attached below as \`Regression-Test-Results.zip\`" >> release-notes.txt
          echo "- **JaCoCo Coverage Report**: Attached below as \`Regression-Test-Coverage-Report.zip\`" >> release-notes.txt

      - name: Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: RupeeCRM v1.0.${{ github.run_number }}
          body_path: release-notes.txt
          files: |
            dist/billsoft.war
            dist/billsoft.war.sha256
            dist/RupeeCRM-Windows-x64.zip
            dist/RupeeCRM-Setup.msi
            dist/RupeeCRM-macOS-arm64.dmg
            dist/RupeeCRM-macOS-x64.dmg
            dist/Regression-Test-Coverage-Report.zip
            dist/Regression-Test-Results.zip
          generate_release_notes: false
          fail_on_unmatched_files: false